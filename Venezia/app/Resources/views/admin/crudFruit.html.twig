{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <h3 class="display-3">Fruit</h3>
        <div class="row">
            <div class="col-md-6">
                {{ form_start(form) }}
                {{ form_row(form.naam) }}
                {#{{ form_row(form.image) }}#}
                {{ form_row(form.seizoen) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}